# Web is deployed standalone to Vercel (Root Directory = web/) where no
# pnpm lockfile exists, so Vercel installs with npm. npm is strict about
# peer dependencies while the local pnpm workspace only warns; allow the
# legacy resolution behavior for the brochure site's lint toolchain.
legacy-peer-deps=true
